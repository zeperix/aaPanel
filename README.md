# aaPanel 7.0.11 Pro
```
URL=https://raw.githubusercontent.com/zeperix/aaPanel/main/install.sh && if [ -f /usr/bin/curl ];then curl -ksSO "$URL" ;else wget --no-check-certificate -O install.sh "$URL";fi;bash install.sh aapanel
```